#include <bits/stdc++.h>
using namespace std;

int main() 
{
    
    int a,b; cin >> a >> b;
    // 1 by using third variable
    // a = a+b;
    // b = a-b;
    // a = a-b;
    // cout << a << " " << b << endl;
    
    // swap(a,b);
    // cout << a <<" " << b << endl;
    
    // a = a ^ b; 
    // b = b ^ a;   b^a^b  => b = a;
    // a = a ^ b;   a^b^a=>  a = b;
    
    // cout << a <<" " << b << endl;
    
    
    
    
    return 0;
}
